Run `nix develop` to enter a shell which installs and activates a Python virtual environment and installs the packages in `requirements.txt` into it.
Further, a variable which numpy needs is expoted.
